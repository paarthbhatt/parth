"use client"

/**
 * Placeholder for the 3D intro. The intro agent replaces this file (and adds
 * siblings in components/intro/). Contract: see the redesign spec — a
 * self-contained <section id="intro"> that ends where #main-content begins.
 */
export function Intro() {
  return <section id="intro" aria-label="Intro" style={{ height: "100vh", background: "#000" }} />
}
